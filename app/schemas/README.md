Gmail → Workers → Parsing → Classification → Correlation → Events
                                          ↓
                                    Event Builder
                                          ↓
                               Event ORM → Event Schema → FastAPI JSON